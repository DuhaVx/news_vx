["project"]
name = "news_vx"
version = "0.1.0"
description = "Django news publishing app managed with uv and SQLite"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "Django>=5.1,<6.0",
    "django-environ>=0.11",
]

["build-system"]
requires = ["hatchling"]
build-backend = "hatchling.build"

["tool.uv"]
dev-dependencies = []
package = false

["tool.hatch.build.targets.wheel"]
packages = []

["tool.hatch.build.targets.sdist"]
include = []

